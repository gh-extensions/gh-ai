# gh-agent
A GitHub CLI extension that adds AI-powered agents to your GitHub workflow — generate commits, review PRs, explain code, and run programmable agent commands using providers like Claude and Gemini.
