Explain what this pull request does in clear, accessible language.

<pr>
Title: ${GH_PR_TITLE}
Branch: ${GIT_BRANCH}
</pr>

<current_description>
${GH_PR_BODY}
</current_description>

<diffstat>
${GIT_DIFF_STAT}
</diffstat>

<diff>
${GIT_DIFF}
</diff>

<context>
<recent_commits>
${GIT_COMMITS}
</recent_commits>
</context>

<format>
- Focus on *what* changed and *why*, not low-level code details
- Organize by logical change group, not by file
- 3–8 bullets per section
- Body: GitHub-flavored Markdown; reference code as `file.ext:line`
- Omit sections that do not apply; do not write "N/A"
- Output only the explanation, no preamble or commentary
- Do NOT include a title line — the entire response is the explanation body
- Wrap prose paragraphs at 120 characters; do not wrap headings, code blocks, inline code, or URLs
</format>

<body>
## Summary

{1–2 sentence overview of the PR's purpose}

## What Changed

{bulleted list of logical changes, grouped by feature or concern}

## Impact

- **Who is affected:** {users, developers, CI, etc.}
- **Breaking changes:** {yes/no with details}
- **Dependencies:** {new, removed, or updated dependencies}

</body>
